export default class CustomConnectyCubeUser {
    constructor(user) {
      this.login = user.login
      this.full_name = user.full_name
      this.phone = user.phone
      this.email = user.email
      this.password = user.password
    }
  }